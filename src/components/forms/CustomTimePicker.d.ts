interface CustomTimePickerProps {
    name: "pickupTime";
    label: string;
    placeholder: string;
}
export default function CustomTimePicker({ name, label, placeholder, }: CustomTimePickerProps): import("react").JSX.Element;
export {};
