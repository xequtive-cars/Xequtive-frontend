interface CustomDatePickerProps {
    name: "pickupDate";
    label: string;
    placeholder: string;
}
export default function CustomDatePicker({ name, label, placeholder, }: CustomDatePickerProps): import("react").JSX.Element;
export {};
